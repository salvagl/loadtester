# Test Dependencies
# Install with: pip install -r requirements.txt -r requirements-test.txt

# Testing Framework
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Async SQLite support (needed for in-memory testing)
aiosqlite>=0.19.0

# HTTP testing
httpx>=0.25.2

# YAML support (if not in main requirements)
pyyaml>=6.0.1
